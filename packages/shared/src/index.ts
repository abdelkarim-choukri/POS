/**
 * @pos/shared — shared types and DTOs between backend and frontend.
 *
 * The backend defines the contract; the frontend imports from here.
 * Add module-specific types in src/api/<module>.ts as features land.
 */

export const POS_SHARED_VERSION = '1.0.0';
